# カンバンTodoアプリ タスク一覧

## 凡例

- ✅ 完了
- 🔲 未着手

## フェーズ 1: 共有型定義・データベースセットアップ

### Issue #2: 共有型定義の追加

- [x] `shared/src/types/model/task.ts` に Status, Task 型を定義
- [x] `shared/src/types/api/task.ts` に API リクエスト/レスポンス型を定義
- [x] `shared/src/index.ts` に re-export を追加

### Issue #3: D1 セットアップ・テスト基盤・Bindings型定義

- [x] `backend/db/migrations/0001_create_tasks_table.sql` を作成
- [x] `backend/wrangler.jsonc` に D1 バインディングを追加
- [ ] `backend/src/index.ts` に Bindings 型定義・グローバルエラーハンドラを追加
- [ ] `@cloudflare/vitest-pool-workers` をインストール、`backend/vitest.config.ts` を `defineWorkersConfig` に書き換え
- [ ] `wrangler d1 migrations apply DB --local` でローカルにマイグレーション適用

> ※ Cloudflareへのデプロイはスコープ外。リモートD1作成・`database_id`設定は不要。

## フェーズ 2: バックエンド API

### Issue #4: Task CRUD ユースケースの実装

- [ ] `backend/src/usecases/task/_tasks.ts` — TaskRow 型、toTask() マッピング関数（DB snake_case → モデル camelCase）
- [ ] `backend/src/usecases/task/get_usecase.ts` — 全タスク取得 + テスト
- [ ] `backend/src/usecases/task/detail_usecase.ts` — 詳細取得 + テスト
- [ ] `backend/src/usecases/task/create_usecase.ts` — 作成（crypto.randomUUID(), order 自動設定, title trim・バリデーション, description 500文字以内） + テスト
- [ ] `backend/src/usecases/task/update_usecase.ts` — 更新（updated_at 手動更新, title/description バリデーション） + テスト
- [ ] `backend/src/usecases/task/delete_usecase.ts` — 削除（同一カラム order 再計算） + テスト
- [ ] `backend/src/usecases/task/move_usecase.ts` — ステータス・並び順変更（db.batch() で原子的 order 再計算, status/order バリデーション） + テスト（同一カラム内移動, カラム間移動, 先頭・末尾への移動）

### Issue #5: Task コントローラーの実装

- [ ] `backend/src/controllers/task_controller.ts` — GET /tasks, GET /tasks/:id, POST /tasks, PUT /tasks/:id, DELETE /tasks/:id, PATCH /tasks/:id/move
- [ ] `backend/src/index.ts` にタスクルートを追加
- [ ] コントローラーの統合テスト

## フェーズ 3: フロントエンド — データ取得・操作フック

### Issue #6: タスクサービスフックの実装

- [x] `frontend/src/pages/kanban/services/constants.ts` — API パス定数
- [x] `frontend/src/pages/kanban/services/useTasks.ts` — 一覧取得フック
- [x] `frontend/src/pages/kanban/services/useCreateTask.ts` — 作成フック
- [x] `frontend/src/pages/kanban/services/useUpdateTask.ts` — 更新フック
- [x] `frontend/src/pages/kanban/services/useDeleteTask.ts` — 削除フック
- [x] `frontend/src/pages/kanban/services/useMoveTask.ts` — 移動フック（楽観的更新: onMutate/onError/onSettled）
- [x] `frontend/src/test/msw/handlers.ts` にタスク用 MSW ハンドラーを追加
- [x] サービスフックのテスト

## フェーズ 4: フロントエンド — UI コンポーネント

### Issue #7: カンバンボード UI コンポーネントの実装

- [ ] `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` をインストール
- [ ] `frontend/src/pages/kanban/components/TaskCard.tsx` — タスクカード + Storybook
- [ ] `frontend/src/pages/kanban/components/KanbanColumn.tsx` — カンバンカラム + Storybook
- [ ] `frontend/src/pages/kanban/components/TaskCreateModal.tsx` — 作成モーダル（Form.Item rules でバリデーション）
- [ ] `frontend/src/pages/kanban/components/TaskEditModal.tsx` — 編集モーダル（削除確認ダイアログ付き）
- [ ] `frontend/src/pages/kanban/KanbanBoardPage.tsx` — カンバンボードページ（DndContext/SortableContext/DragOverlay 構成、ローディング/エラー/空状態の表示）

## フェーズ 5: ルーティング・統合

### Issue #8: ルーティング統合・テスト

- [ ] `frontend/src/routes/index/route.tsx` を更新してカンバンボードページを表示
- [ ] カンバンボードの統合テスト（空のボード表示、タスクあり表示、作成モーダル送信、削除確認ダイアログ）
- [ ] DnD 操作の統合テスト（カラム間移動、同一カラム内並び替え、楽観的更新・ロールバック）

## フェーズ 6: 品質保証・仕上げ

### Issue #9: ドキュメント・設定の更新

- [ ] `README.md` を更新
- [ ] `.github/copilot-instructions.md` のプロジェクト内容を更新

### Issue #1: ドキュメント整備

- [x] `docs/01_要件.md` を作成
- [x] `docs/02_仕様.md` を作成
- [x] `docs/03_計画.md` を作成
- [x] `docs/03_01_PR計画.md` を作成
- [x] `docs/04_タスク一覧.md` を作成

## 各フェーズ完了後作業

### CodeSimplifier 実行

サブエージェントとして #tool:agent/runSubagent を使用し、CodeSimplifier を実行
対象ファイルとして、各フェーズで作成、変更したファイルを指定する

### コマンド実行

```bash
bun run biome:write
bun run typecheck
bun run test:run
```

### PR 作成

commit-pr スキルを参考に、コミット、PR作成を実行する